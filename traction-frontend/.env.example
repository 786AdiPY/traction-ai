# API origin (no trailing slash). If empty, dev uses same-origin /v1 (Vite proxy → backend).
# VITE_API_BASE=http://localhost:8080

# Optional: Anthropic for synthesis (or set in Settings)
# VITE_ANTHROPIC_API_KEY=
