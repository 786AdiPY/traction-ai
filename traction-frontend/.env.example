# API origin — no trailing slash.
# Local: leave empty so Vite proxies /api and /v1 to localhost:8080 (see vite.config.js).
# VITE_API_BASE=

# Production (Render backend) — also set in Vercel Project → Environment Variables if you override vercel.json.
# VITE_API_BASE=https://traction-ai-backend-h97q.onrender.com
